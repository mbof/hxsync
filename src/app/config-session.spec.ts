import { ConfigSession } from './config-session';
import { DevicemgrService } from './devicemgr.service';

describe('ConfigSession', () => {
  it('does cool stuff', () => {
    // TODO: write some tests against a local device mock.
  });
});
