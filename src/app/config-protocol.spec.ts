import { ConfigProtocol } from './config-protocol';

describe('ConfigProtocol', () => {
  it('does cool stuff'),
    () => {
      // TODO: implement tests against a low-level device emulator.
    };
});
